<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.akquinet.android.marvintest" android:versionCode="1"
    android:versionName="1.0"
>
    <application android:icon="@drawable/icon"
        android:label="Marvin Tests" android:debuggable="true"
    >

        <uses-library android:name="android.test.runner" />

        <activity android:name=".activities.ActivityA"></activity>
        <activity android:name=".activities.ActivityB"></activity>
        <activity android:name=".activities.ActivityC"></activity>

        <service android:name=".services.AdderAndroidService"></service>
    </application>

    <uses-sdk android:minSdkVersion="4" />

    <instrumentation android:targetPackage="de.akquinet.android.marvintest"
        android:name="android.test.InstrumentationTestRunner" />

</manifest>
